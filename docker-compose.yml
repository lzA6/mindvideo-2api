# /docker-compose.yml
services:
  nginx:
    image: nginx:latest
    container_name: mindvideo-2api-nginx
    restart: always
    ports:
      - "${NGINX_PORT:-8088}:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app
    networks:
      - mindvideo-net

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mindvideo-2api-app
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - mindvideo-net

networks:
  mindvideo-net:
    driver: bridge
