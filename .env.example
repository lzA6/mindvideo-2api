# ====================================================================
# mindvideo-2api 配置文件模板
# ====================================================================
#
# 请将此文件重命名为 ".env" 并填入您的凭证。
#

# --- 核心安全配置 (必须设置) ---
# 用于保护您 API 服务的访问密钥。
API_MASTER_KEY=sk-mindvideo-2api-default-key-please-change-me

# --- 部署配置 (可选) ---
# Nginx 对外暴露的端口
NGINX_PORT=8088

# --- MindVideo.ai 凭证 (必须设置) ---
# 请从浏览器开发者工具中获取完整的 Bearer Token (不包含 "Bearer " 前缀)。
# 支持多账号轮询，只需按格式添加 MINDVIDEO_AUTH_TOKEN_2, MINDVIDEO_AUTH_TOKEN_3, ...
MINDVIDEO_AUTH_TOKEN_1="在此处粘贴您的 Bearer Token"

# --- 签名密钥 (通常无需修改) ---
# 这是通过分析前端JS代码获得的静态密钥，用于生成 i-sign。
SIGN_APP_KEY=s#c_120*AB
